apiVersion: v1
kind: Service
metadata:
  name: hello-camel-service
  namespace: default
spec:
  selector:
    app: hello-camel-service
  ports:
    - port: 80
      targetPort: http
  type: ClusterIP
